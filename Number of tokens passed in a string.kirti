#include<bits/stdc++.h>
using namespace std;

int tokens(string s) {
    map<int,int> m;
    for(char x:s){
        m[x-'a']++;
    }
    return m.size();
}

int main(){
    string name;
    cin>>name;
    cout<<tokens(name);
    return 0;    
}
