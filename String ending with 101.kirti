#include<bits/stdc++.h>
using namespace std;

bool valid(string s)
{
    int n=s.size();
    if(s[n-1]=='1'&&s[n-2]=='0'&&s[n-3]=='1')
            return true;
    return false;
}

int main()
{
    string name;
    cin>>name;
    if(valid(name))
        cout<<"Accepted";
    else
        cout<<"NOT Accepted";
}
